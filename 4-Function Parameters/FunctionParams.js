var max = function(a,b){
    if(a>b){
	  return a;
	}
	return b;

}

// if more params are passed, it will ignore the third parameter
//if less parameters are passed , it will make the left out parameter as undefined.
